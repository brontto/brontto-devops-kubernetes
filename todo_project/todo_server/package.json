{
  "name": "todo_server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rmdir /s /q build && cd ../todo_front && npm run build && move build ../todo_server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Henri Virtanen",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
